import { ValidacionesTaller } from './validaciones-taller';

describe('ValidacionesTaller', () => {
  it('should create an instance', () => {
    expect(new ValidacionesTaller()).toBeTruthy();
  });
});
